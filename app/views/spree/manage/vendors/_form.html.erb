<% if @account.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

    <ul>
    <% @account.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.label :name %><br />
  <%= f.text_field :name %>
</div>

<div class="field">
  <%= f.label :email %><br />
  <%= f.email_field :email %>
</div>

<div class="field">
  <%= f.label :order_cutoff_time %><br />
  <%= f.text_field :order_cutoff_time %>
</div>

<div class="field">
  <%= f.label :delivery_minimum %><br />
  <%= f.number_field :delivery_minimum, :value => (number_with_precision(f.object.delivery_minimum, :precision => 0)) %>
</div>

<div class="field">
  <%= f.label :payment_terms %><br />
  <%= f.number_field :payment_terms %>
</div>

<div class="actions">
  <%= f.submit "Update account", class: "update-customer"  %>
</div>
